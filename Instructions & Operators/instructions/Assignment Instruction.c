#include <stdio.h>

int main() {
    int a, b;
    
    a = 10;   // assignment instruction
    b = 20;   // assignment instruction

    printf("a = %d, b = %d\n", a, b);  // output instruction

    return 0;
}