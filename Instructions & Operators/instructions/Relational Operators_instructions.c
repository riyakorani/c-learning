#include <stdio.h>

int main() {
    int a = 8, b = 5;

    printf("a > b : %d\n", a > b);    // relational instruction
    printf("a < b : %d\n", a < b);
    printf("a == b: %d\n", a == b);

    return 0;
}